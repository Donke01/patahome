{
  "name": "patahome-backend",
  "version": "0.1.0",
  "description": "PataHome API — listings marketplace for homes, land & vehicles in Kenya. Zero dependencies.",
  "main": "server.js",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node seed.js"
  }
}
